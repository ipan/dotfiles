[user]
    name = Ivan Pan
    email = me@ivanpan.com

[core]
    editor = vim
    quotepath = false

[log]
    date = local

[color]
    branch = auto
    diff = auto
    interactive = auto
    status = auto
    ui = true

[alias]
    st = status
    co = checkout
    up = pull --rebase
    mlog = log --no-merges
    ls = log --name-only -- .
    sh = show --name-only
    cp = cherry-pick -x
    cpx = cherry-pick -x -X theirs

[push]
    default = simple

